apiVersion: v1
kind: Service
metadata:
  name: {{ include "vision-one-mcp.fullname" . }}
  labels:
    {{- include "vision-one-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vision-one-mcp.selectorLabels" . | nindent 4 }}
